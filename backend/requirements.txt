fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
beautifulsoup4>=4.12,<5.0
pydantic>=2.7,<3.0
# Case intake. Optional at runtime: with no ANTHROPIC_API_KEY the intake
# endpoint falls back to keyword matching and says so.
anthropic>=0.75,<1.0
